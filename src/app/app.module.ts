import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';

@NgModule({
  imports: [CommonModule], // solo módulos, nada de componentes standalone
})
export class AppModule {}
